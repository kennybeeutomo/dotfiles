require("monitors") -- Monitors
require("autostart") -- Autostart
require("fixes") -- Fixes
require("decorations") -- Look and Feel
require("animations") -- Animations
require("input") -- Input
require("binds") -- Keybindings
require("window_rules") -- Window rules
require("workspace_rules") -- Workspace rules
require("hidden") -- Hidden configs
